#!/usr/bin/env bash

# shellcheck disable=SC1091
source "${DOTFILES_DIR}/bash/test/platform.inc"

setUp() {
    unset CDPATH
    cd "${DOTFILES_DIR}/bash" || return

    # shellcheck disable=SC1091
    source ".bash_topics.d/git.bash"
}

# cdtop
# pushdtop
# since_commit
# in_commit
# dirty
# edit_since
# edit_commit
# edit_dirty
# fzf_git_show?

# shellcheck disable=SC1090
source "$(which shunit2)"
