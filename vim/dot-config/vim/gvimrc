if has('mac')
    set guifont=JetBrainsMono-Regular:h20
    set macligatures
else
    silent! call simple_guifont#Set(['JetBrainsMono Nerd Font'], 'Monospace', 14)
endif
set columns=148
set lines=84

highlight HighlightedyankRegion guifg=Black guibg=Yellow
