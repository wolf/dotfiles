theme = "onelight"

[editor]
bufferline = "multiple"
color-modes = true
default-yank-register = "+"
end-of-line-diagnostics = "hint"
rulers = [120]
scrolloff = 3
shell = ["bash", "-c"]
true-color = true

[editor.cursor-shape]
insert = "bar"
normal = "block"
select = "underline"

[editor.whitespace]
render = "none"

[editor.statusline]
left = ["mode", "spinner", "version-control"]
center = ["file-name", "read-only-indicator", "file-modification-indicator"]
right = ["diagnostics", "selections", "register", "position", "position-percentage", "file-encoding"]
mode.normal = "NORMAL"
mode.insert = "INSERT"
mode.select = "SELECT"

[editor.inline-diagnostics]
cursor-line = "warning"

[keys.normal.backspace]
a = ":set line-number absolute"
r = ":set line-number relative"
l = ":set whitespace.render all"
L = ":set whitespace.render none"
